h1 TwitterFace
h2 Search
input(placeholder="Search")
h2 New Friends
table.table
  tr
    td(ng-repeat="(id, person) in profile.person")
      a(href="#/people/{{id}}")
        img(ng-src="{{person.photo}}")
